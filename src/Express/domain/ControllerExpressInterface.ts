/**
 * ControllerExpressInterface serves as a base interface for all
 * controllers in an Express application. It can be extended
 * to define common methods or properties for controllers.
 */
export default interface ControllerExpressInterface {
    // This interface can be extended with common methods or properties for controllers.
  }